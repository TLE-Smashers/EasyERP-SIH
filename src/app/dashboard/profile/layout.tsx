import type { ReactNode } from "react";

/**
 * Profile Layout
 * Provides consistent structure for profile-related pages
 */
export default function ProfileLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
