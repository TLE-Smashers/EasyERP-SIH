import type { ReactNode } from "react";

/**
 * Settings Layout
 * Provides consistent structure for settings pages
 */
export default function SettingsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
